# Truncate = keep<dip(\+=@ .)
Truncate ← ▽<⊙(\+=@ .)

⍤. ≅ "Hello how are you" Truncate 4 "Hello how are you Contestant"
⍤. ≅ "What is the solution" Truncate 4 "What is the solution to this problem"
⍤. ≅ "chopper is not a tanuki" Truncate 5 "chopper is not a tanuki"
